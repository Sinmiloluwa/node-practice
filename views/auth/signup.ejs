<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Login</title>
    <link rel="stylesheet" href="/css/product.css">
    <link rel="stylesheet" href="/css/forms.css">
</head>
<body>
    <%- include('../includes/nav.ejs') %>
    <h1>Signup</h1>
    <% if (errorMessage) { %>
        <p class="error-message"><%= errorMessage %></p>
    <% } %>
    <form class="login-form" action="/signup" method="POST" novalidate>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="form-control">
            <label for="username">Username:</label>
            <input class="<%= validationErros.find(e => e.param === 'username') ? 'invalid' : '' %>" type="text" id="username" name="username" value="<%= oldInput.username %>" required>
        </div>
        <div class="form-control">
            <label for="email">Email:</label>
            <input class="<%= validationErros.find(e => e.param === 'email') ? 'invalid' : '' %>" type="email" id="email" name="email" value="<%= oldInput.email %>" required>
        </div>
        <div class="form-control">
            <label for="password">Password:</label>
            <input class="<%= validationErros.find(e => e.param === 'password') ? 'invalid' : '' %>" type="password" id="password" name="password"  value="<%= oldInput.password %>" required>
        </div>
        <button type="submit">Signup</button>
    </form>
</body>
</html>